# ========================
# Rutas absolutas a proyectos
# ========================
# En Windows, usa rutas absolutas con letra de unidad (los forward slashes funcionan bien)
USUARIOS_DIR=C:/Users/ismae/Documents/MASTER UAH/TFM Bandas/usuarios
EVENTS_DIR=C:/Users/ismae/Documents/MASTER UAH/TFM Bandas/eventos
GATEWAY_DIR=C:/Users/ismae/Documents/MASTER UAH/TFM Bandas/gateway
IDENTITY_DIR=C:/Users/ismae/Documents/MASTER UAH/TFM Bandas/identity

# ========================
# Credenciales MySQL
# ========================
# Base de datos de usuarios
MYSQL_ROOT_PASSWORD=root
USERS_DB=tfm_users
USERS_USER=tfm_users_rw
USERS_PASS=tfm_users_password

# Base de datos de eventos
EVENTS_DB=tfm_events
EVENTS_USER=tfm_events_rw
EVENTS_PASS=tfm_events_password

# ========================
# Realm/Keycloak
# ========================
KC_REALM=tfm-bandas
KC_URL_INTERNAL=http://keycloak:8080
KC_JWKS=${KC_URL_INTERNAL}/realms/${KC_REALM}/protocol/openid-connect/certs
KC_ADMIN_CLIENT_ID=realm-admin
KC_ADMIN_CLIENT_SECRET=8nWwBGuo0RxdRQfHUaQgcqv7Fibt8JYX

# ========================
# Puertos de microservicios
# ========================
KEYCLOAK_PORT=8080
USERS_PORT=8081
SCORES_PORT=8082
EVENTS_PORT=8083
SURVEYS_PORT=8084
GATEWAY_PORT=8085
IDENTITY_PORT=8086

MYSQL_PORT=3307
KEYCLOAK_MYSQL_PORT=3308


# ========================
# Gateway y microservicios
# ========================
# URIs internas de los microservicios dentro del entorno Docker
GATEWAY_USERS_URI=http://usuarios:8080
GATEWAY_EVENTS_URI=http://eventos:8080
GATEWAY_SCORES_URI=http://partituras:8080
GATEWAY_SURVEYS_URI=http://encuestas:8080

# Paths gestionados por el gateway (mismo formato que en local)
GATEWAY_USERS_PATH=/api/users/**
GATEWAY_INSTRUMENTS_PATH=/api/instruments/**
GATEWAY_EVENTS_PATH=/api/events/**
GATEWAY_SCORES_PATH=/api/scores/**
GATEWAY_SURVEYS_PATH=/api/surveys/**